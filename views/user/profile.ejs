<div class="container py-5">
    
    <header class="text-center mb-5">
        <h1 class="display-4 fw-bold text-primary">Welcome, <%= user.name %> 👋</h1>
<p class="lead" style="color:#4da3ff;">Your Dashboard for the Current Election</p>
    </header>

    <div class="row g-4">
        
        <div class="col-md-6">
            <div class="card shadow-lg border-0 h-100 rounded-4">
                <div class="card-body p-5">
                    <h3 class="card-title fw-bold mb-4">🗳️ Your Voting Details</h3>
                    <hr class="mb-4">

                    <div class="d-flex align-items-center mb-4">
                        <i class="bi bi-person-badge fs-3 me-3 text-info"></i> 
                        <div>
                            <p class="text-uppercase small mb-0 text-muted">Aadhar/Voter ID</p>
                            <p class="h5 fw-bold mb-0 "><%= user.aadharCard %></p>
                        </div>
                    </div>

                    <div class="d-flex align-items-center">
                        <i class="bi bi-patch-check fs-3 me-3 
                        <% if (user.isVoted) { %>text-success<% } else { %>text-danger<% } %>"></i>
                        
                        <div>
                            <p class="text-uppercase small mb-0 text-muted">Voting Status</p>
                            <% if (user.isVoted) { %>
                                <p class="h5 fw-bold mb-0 text-success">
                                    You have already voted <span class="ms-2">🎉</span>
                                </p>
                            <% } else { %>
                                <p class="h5 fw-bold mb-0 text-danger">
                                    You have not voted yet <span class="ms-2">🔔</span>
                                </p>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>
        </div>


        <div class="col-md-6">
            <div class="card shadow-lg border-0 h-100 rounded-4">
                <div class="card-body p-5 d-flex flex-column">
                    <h3 class="card-title fw-bold mb-4">🚀 Quick Actions</h3>
                    <hr class="mb-4">

                    <div class="mb-3 flex-grow-1 d-flex flex-column justify-content-center">
                        <% if (!user.isVoted) { %>
                            <a href="/candidatelist" class="btn btn-primary btn-lg w-100 shadow-sm py-3 rounded-pill action-btn hover-primary">
                                <i class="bi bi-box-arrow-right me-2"></i> Vote Now 🗳️
                            </a>
                        <% } else { %>
                            <button class="btn btn-secondary btn-lg w-100 py-3 rounded-pill" disabled>
                                <i class="bi bi-check-circle me-2"></i> Already Voted 🗳️
                            </button>
                            <p class="text-center mt-2 text-muted small">Thank you for participating!</p>
                        <% } %>
                    </div>
                    
                    <div class="d-grid gap-3 mt-4">
                        <a href="/results" class="btn btn-outline-warning w-100 py-3 rounded-pill action-btn hover-warning">
                            <i class="bi bi-bar-chart-line me-2"></i> View Live Results 📊
                        </a>
                        <a href="/userdata" class="btn btn-outline-dark w-100 py-3 rounded-pill action-btn hover-dark">
                            <i class="bi bi-person me-2"></i> Your Profile 👤
                        </a>
                    </div>
                </div>
            </div>
        </div>

    </div>
</div>