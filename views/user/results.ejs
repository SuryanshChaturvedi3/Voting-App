<div class="container py-5">

    <h1 class="text-center fw-bold mb-5 text-primary">Final Election Results 🗳️</h1>

    

    <!-- Winner Card -->
    <div class="card shadow-lg border-success border-3 mb-5">
        <div class="card-body text-center p-5">
            <h2 class="fw-bold text-success">🏆 Winner</h2>

            <h3 class="display-6 fw-bold mt-3"><%= winner.name %></h3>

            <p class="lead mt-2 text-muted">
                <%= winner.voteCount %> votes secured 🎉
            </p>
        </div>
    </div>

    <!-- All Candidates -->
    <h3 class="fw-bold mb-3">All Candidates</h3>

    <table class="table table-striped table-bordered shadow-sm">
        <thead class="table-dark">
            <tr>
                <th>Name</th>
                <th>Votes</th>
            </tr>
        </thead>
        <tbody>
            <% candidates.forEach(c => { %>
                <tr>
                    <td><%= c.name %></td>
                    <td><%= c.voteCount %></td>
                </tr>
            <% }) %>
        </tbody>
    </table>

</div>
<style>
    body {
        background-color: #f8f9fa;
    }
</style>