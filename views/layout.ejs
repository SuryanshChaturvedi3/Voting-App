<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %> | VoteApp</title>

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />

  <!-- Modern UI Styling -->
  <style>
    body {
      background: linear-gradient(135deg, #1d1e22, #2d2f36);
      color: #f1ecec;
      font-family: 'Inter', sans-serif;
      min-height: 100vh;
    }

    .navbar-custom {
      background: rgba(20, 20, 20, 0.75);
      backdrop-filter: blur(8px);
      padding: 15px 30px;
    }

    .card-custom {
      background: #3d3e40;
      border: none;
      border-radius: 18px;
      padding: 30px;
      box-shadow: 0px 10px 30px rgba(0, 0, 0, 0.45);
      transition: 0.3s;
    }

    .card-custom:hover {
      transform: translateY(-6px);
      box-shadow: 0px 15px 35px rgba(0, 0, 0, 0.5);
    }

    .form-control {
      height: 50px;
      border-radius: 12px;
      background: #cdcfd6;
      border: 1px solid #ece6e6;
      color: #0d0b0b;
    }

    .form-control:focus {
      background: #edeff5;
      color: #0d0b0b;
      border-color: #6ea8fe;
      box-shadow: none;
    }

    .btn-custom {
      height: 52px;
      border-radius: 12px;
      font-size: 1.05rem;
      font-weight: 600;
    }

    .container-custom {
      margin-top: 60px;
      margin-bottom: 60px;
      max-width: 900px;
    }
  </style>
</head>

<body>
  <nav class="navbar navbar-expand-lg navbar-dark navbar-custom">
    <a class="navbar-brand fw-bold" href="/">VoteApp</a>
  </nav>

  <div class="container container-custom">
    <div class="card-custom">
      <%- body %>
    </div>
  </div>
</body>
</html>
